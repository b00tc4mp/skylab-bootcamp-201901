{
  "name": "stayfit",
  "version": "1.0.0",
  "description": "App to manage box, gym and personal trainer session schedules",
  "main": "server/index.ts",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run start --prefix app\"",
    "dev:api": "npm run dev --prefix server",
    "dev:app": "npm run dev --prefix server",
    "server:test": "npm test --prefix server",
    "server:test:debug": "npm run test:debug --prefix server"
  },
  "author": "Pablo Javier Rey Sobral",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}
