// business logic

function register(email, password) {
    console.log('register', email, password);
}

function registerAdmin(email, password) {
    console.log('register admin', email, password);
}

function registerSuperAdmin(email, password) {
    console.log('register super admin', email, password);
}