extends ../tracks/index

block track
    if track
        section(class="track")
            h2 Track
                div 
                    h4 #{track.name} 
                    if track.preview_url
                        audio(controls='', autoplay='', loop='', src=track.preview_url)  
                    else
                        p Sorry, there is no preview available
