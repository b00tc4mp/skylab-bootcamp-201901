
mixin favorites(ducks)
        h2 Favorites
        ul
            for item in ducks
                li
                    a(href=item.url)
                        h2=item.title
                        img(src=item.image)
                        span=item.price
