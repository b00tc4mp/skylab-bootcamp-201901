// business logic

function register(email, password) {
    console.log('Sign In', email, password);
}

function registerAdmin(email, password) {
    console.log('Sign In Admin', email, password);
}

function registerSuperAdmin(email, password) {
    console.log('Sign In Super Admin',email, password);
}