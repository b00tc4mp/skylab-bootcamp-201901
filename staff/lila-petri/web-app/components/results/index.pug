mixin results()
    ul
        for item in ducks
            li
                a(href=item.url)
                    h2=item.title
                    img(src=item.image)
                    span=item.price
