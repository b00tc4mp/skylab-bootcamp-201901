{
  "name": "dashboard-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "argon2": "^0.23.0",
    "body-parser": "^1.19.0",
    "dashboard-call": "file:../dashboard-call",
    "dashboard-errors": "file:../dashboard-errors",
    "dashboard-normalize": "file:../dashboard-normalize",
    "dashboard-validate": "file:../dashboard-validate",
    "dotenv": "^8.0.0",
    "moment": "^2.24.0",
    "moment-random": "^1.0.5",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-router-dom": "^5.0.0",
    "react-scripts": "3.0.1",
    "validator": "^11.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "mocha ./src/**/*.test.js --runInBand --require mock-local-storage",
    "test:debug": "mocha debug ./src/**/*.test.js --runInBand",
    "test:coverage": "nyc --reporter=html --reporter=text mocha ./src/**/*.test.js --runInBand --require mock-local-storage"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "dashboard-data": "file:../dashboard-data",
    "mocha": "^6.1.4",
    "mock-local-storage": "^1.1.8",
    "nyc": "^14.1.1"
  }
}
