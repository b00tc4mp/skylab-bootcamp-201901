include ../search/index

mixin nav(visualSearch)
    nav(class='navbar navbar-light bg-light searchPanel')
        img(src='/images/logoSputnikfy.png', width='200px')
        if visualSearch
            +search()
        form(method='POST', action='/logout')
            button(class='btn btn-outline-dark') Logout

