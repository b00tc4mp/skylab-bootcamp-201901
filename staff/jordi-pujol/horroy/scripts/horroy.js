function Horroy(){

    if ((arguments.length === 1) && (Number.isInteger(arguments)) thro TypeError ('it has to be integer')
    // if (arguments.length)

    this.length = this.arguments

    if (arguments.length){
        for (var i =0; i< arguments.length; i++){
            this[i] = arguments[i]
        }
    }
}