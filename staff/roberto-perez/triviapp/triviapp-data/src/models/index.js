'use strict';

const { AnswerGame } = require('./answer-game.model');
const { Answer } = require('./answer.model');
const { Question } = require('./question.model');
const { Quiz } = require('./quiz.model');
const { User } = require('./user.model');

module.exports = {
    AnswerGame,
    Answer,
    Question,
    Quiz,
    User
}
