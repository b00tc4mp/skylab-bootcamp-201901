/**
 * Abstracting of splice
 * 
 * method changes the contents of an array by 
 * removing or replacing existing elements and/or adding new elements.
 * 
 * @param {number} start - Index to start changing the array. 
 * If greather than length it will be array.length
 * If negative 
 * 
 * 
 * @returns {Array} - An array containing the deleted elements. 
 * If only one element is removed, an array of one element is returned. 
 * If no elements are removed, an empty array is returned.
 */

 function splice(array, start){
   // debugger;
   
 }